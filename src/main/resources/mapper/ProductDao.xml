<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.ml.aweb.mapper.dao.ProductDao">

    <resultMap type="com.ml.aweb.mapper.entity.Product" id="ProductMap">
        <result property="id" column="id" jdbcType="INTEGER"/>
        <result property="name" column="name" jdbcType="VARCHAR"/>
        <result property="subtitle" column="subTitle" jdbcType="VARCHAR"/>
        <result property="originalprice" column="originalPrice" jdbcType="OTHER"/>
        <result property="promoteprice" column="promotePrice" jdbcType="OTHER"/>
        <result property="stock" column="stock" jdbcType="INTEGER"/>
        <result property="cid" column="cid" jdbcType="INTEGER"/>
        <result property="createdate" column="createDate" jdbcType="TIMESTAMP"/>
    </resultMap>

    <!--查询单个-->
    <select id="queryById" resultMap="ProductMap">
        select id,
               name,
               subTitle,
               originalPrice,
               promotePrice,
               stock,
               cid,
               createDate
        from aweb.product
        where id = #{id}
    </select>

    <!--查询指定行数据-->
    <select id="queryAllByLimit" resultMap="ProductMap">
        select id,
               name,
               subTitle,
               originalPrice,
               promotePrice,
               stock,
               cid,
               createDate
        from aweb.product
        limit #{offset}, #{limit}
    </select>

    <!--通过实体作为筛选条件查询-->
    <select id="queryAll" resultMap="ProductMap">
        select
        id, name, subTitle, originalPrice, promotePrice, stock, cid, createDate
        from aweb.product
        <where>
            <if test="id != null">
                and id = #{id}
            </if>
            <if test="name != null and name != ''">
                and name = #{name}
            </if>
            <if test="subtitle != null and subtitle != ''">
                and subTitle = #{subtitle}
            </if>
            <if test="originalprice != null">
                and originalPrice = #{originalprice}
            </if>
            <if test="promoteprice != null">
                and promotePrice = #{promoteprice}
            </if>
            <if test="stock != null">
                and stock = #{stock}
            </if>
            <if test="cid != null">
                and cid = #{cid}
            </if>
            <if test="createdate != null">
                and createDate = #{createdate}
            </if>
        </where>
    </select>

    <!--新增所有列-->
    <insert id="insert" keyProperty="id" useGeneratedKeys="true">
        insert into aweb.product(name, subTitle, originalPrice, promotePrice, stock, cid, createDate)
        values (#{name}, #{subtitle}, #{originalprice}, #{promoteprice}, #{stock}, #{cid}, #{createdate})
    </insert>

    <insert id="insertBatch" keyProperty="id" useGeneratedKeys="true">
        insert into aweb.product(name, subTitle, originalPrice, promotePrice, stock, cid, createDate)
        values
        <foreach collection="entities" item="entity" separator=",">
            (#{entity.name}, #{entity.subtitle}, #{entity.originalprice}, #{entity.promoteprice}, #{entity.stock},
            #{entity.cid}, #{entity.createdate})
        </foreach>
    </insert>

    <insert id="insertOrUpdateBatch" keyProperty="id" useGeneratedKeys="true">
        insert into aweb.product(name, subTitle, originalPrice, promotePrice, stock, cid, createDate)
        values
        <foreach collection="entities" item="entity" separator=",">
            (#{entity.name}, #{entity.subtitle}, #{entity.originalprice}, #{entity.promoteprice}, #{entity.stock},
            #{entity.cid}, #{entity.createdate})
        </foreach>
        on duplicate key update
        name = values(name) , subTitle = values(subTitle) , originalPrice = values(originalPrice) , promotePrice =
        values(promotePrice) , stock = values(stock) , cid = values(cid) , createDate = values(createDate)
    </insert>

    <!--通过主键修改数据-->
    <update id="update">
        update aweb.product
        <set>
            <if test="name != null and name != ''">
                name = #{name},
            </if>
            <if test="subtitle != null and subtitle != ''">
                subTitle = #{subtitle},
            </if>
            <if test="originalprice != null">
                originalPrice = #{originalprice},
            </if>
            <if test="promoteprice != null">
                promotePrice = #{promoteprice},
            </if>
            <if test="stock != null">
                stock = #{stock},
            </if>
            <if test="cid != null">
                cid = #{cid},
            </if>
            <if test="createdate != null">
                createDate = #{createdate},
            </if>
        </set>
        where id = #{id}
    </update>

    <!--通过主键删除-->
    <delete id="deleteById">
        delete
        from aweb.product
        where id = #{id}
    </delete>

</mapper>